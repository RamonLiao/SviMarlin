tasks/progress.md
tasks/lessons.md

# Claude (per global rule — never commit)
.claude/

# Rust
/target
**/target

# Secrets — NEVER commit (private keys, env). Only .env.example is tracked.
.env
.env.*
!.env.example
